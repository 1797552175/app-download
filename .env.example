# 会话密钥，至少 32 位，上线请务必修改
SESSION_SECRET=qicheng-download-session-secret-change-me-32chars

# 首次启动时若管理员不存在，会用下面账号初始化
ADMIN_USERNAME=huqicheng
ADMIN_PASSWORD=huqicheng

# 本地开发默认写到项目 ./data；Docker 中由 compose 设为 /data
# DATA_DIR=./data

# Docker 对外端口（宿主机端口:容器内 3000）。3000 被占用时可改为 3001 等
APP_PORT=3001
